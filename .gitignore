# Verification captures and deck exports live outside this repo
# (Cargo/refs, Cargo/deliverables).
refs/
.playwright-mcp/
deliverables/
_tmp/
# Stray captures at the repo ROOT only - anchored so it cannot swallow
# assets/*.png, which is where the real logo files live.
/*.png
!og.png
